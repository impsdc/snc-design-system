import React from 'react';

import { Button } from './Button';


export default {
    title: "Component/Button",
    component: Button,
};

export const Default = (args) => {
    return <Button />
};